# Risk parameters that agents can adjust.
# Values here OVERRIDE the base config/config.yaml when present.
# Only the Risk Monitor and Strategy agents should modify this file.

overrides: {}
  # Example overrides (uncomment to activate):
  # risk:
  #   daily_loss_limit_pct: 0.02
  #   min_confidence: 0.60

changelog: []
  # - timestamp: "2026-04-09 14:30:00"
  #   agent: "risk_monitor"
  #   change: "Tightened daily_loss_limit_pct from 0.03 to 0.02"
  #   reason: "VIX above 20, market breadth deteriorating"
